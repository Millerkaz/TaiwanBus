import React from "react";
import Popup from "../../components/popup";

const BusRoutePopup = (props) => {
  return (
    <Popup>
      <div>123</div>
    </Popup>
  );
};

export default BusRoutePopup;

//一律用 routeUID get busGPS,busNext,Shape
